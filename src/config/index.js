const config = {
  api: process.env.REACT_APP_API,
  googleMapKey: process.env.REACT_APP_GOOGLE_MAP_KEY,
};

export const { api } = config;

export const { googleMapKey } = config;

export default config;
